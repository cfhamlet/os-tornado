language: python

sudo: false

python:
  - "2.7"

install:
  - pip install -r requirements.txt
  - pip install -e .
  - pip install pytest
  - pip install coverage
  - pip install codecov

script: 
  - coverage erase
  - coverage run -m py.test
  - coverage combine

after_success:
  - codecov
